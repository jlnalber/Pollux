<UserControl x:Class="Pollux.KantenEllipse"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pollux"
             mc:Ignorable="d" 
             BorderBrush="Transparent"
             BorderThickness="0"
             Background="Transparent"
             d:DesignHeight="40" d:DesignWidth="40">
    <Ellipse x:Name="Ellipse" Fill="Transparent" Stroke="Black" StrokeThickness="2" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="40" Width="40">
        <!--<Ellipse.ContextMenu>
            <ContextMenu>
                <MenuItem x:Name="Eigenschaften"/>
                <MenuItem x:Name="Löschen"/>
                <Separator/>
                <MenuItem x:Name="MenuItem1">
                    <MenuItem x:Name="MenuItem2"/>
                    <MenuItem x:Name="MenuItem3"/>
                </MenuItem>
                <MenuItem x:Name="MenuItem4"/>
            </ContextMenu>
        </Ellipse.ContextMenu>-->
    </Ellipse>
</UserControl>
